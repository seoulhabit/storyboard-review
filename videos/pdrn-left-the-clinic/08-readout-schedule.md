# 08-readout-schedule.md — pdrn-left-the-clinic

Per `[UNDERPOWERED]` note in `01-story-brief.md`: this channel has **zero
public long-form uploads**, so no `curve.*`/`retention.*` comparator exists
yet for this format. The 48h/7d readout below is scheduled per the pipeline's
own convention, but every comparison it produces should be marked
`[UNDERPOWERED]` at S9 rather than read against this channel's Shorts-tuned
baseline.

## Schedule (from publish time, not yet set — Kim's click starts the clock)

- **48-hour check**: views, avg-view-%, traffic source split (long-form has
  no Shorts-feed dominance to expect — different discovery surface entirely)
- **7-day check**: same metrics + first real comparator point for this
  format, going forward

## What NOT to conclude from this run's own numbers

- Any duration-vs-performance claim — this channel's own baseline already
  states duration has no discriminative power on the Shorts side; nothing
  is known yet about long-form.
- Any comparison implying this video under/over-performed a Shorts norm —
  different format, different feed, different audience intent.
