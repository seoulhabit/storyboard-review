Source chip — always-on furniture, present on the first frame and on every scene that states a claim.

```jsx
<ShChip source="CIR 2015" />
```

Normally you get it for free by passing `chip` to `ShScene`. Use `pinned={false}` only to place it inline inside a panel. A claim with no source doesn't ship.
