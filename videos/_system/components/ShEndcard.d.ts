/**
 * sh-endcard — 서울의 습관, the SEOULHABIT wordmark, one CTA line. Nothing else, ever.
 */
export interface ShEndcardProps {
  /** One CTA line. */
  cta: string;
}
export declare function ShEndcard(props: ShEndcardProps): JSX.Element;
