# Open Items Register

Built by WO-SBR-001's T3, from agent-written artefacts and T2's branch-register
analysis across every unmerged branch as of 2026-09-04. This register *lists*
these items; it does not resolve them (WO-SBR-001 §5).

| ID | Item | Source branch | Source | Status | Proposed owner |
|---|---|---|---|---|---|
| 1 | Nine brief (~1.0–1.25s) frames where a mid-scene band reads as empty at a same-background transition (e.g. 10.5–11.75s, s02→s03). Spot-checked as real, not a false positive — the incoming scene's wipe mask covers the outgoing scene's content before its own headline fades in enough to read. Disclosed by its own author as "a real, fixable rough edge, not a false alarm." Passes the pixel-level cadence gate regardless. | `wip/hyaluronic-acid-video-rewrite-1ab509-2026-09-04` | `videos/hyaluronic-acid-vs-filler/BEFORE-AFTER-REPORT.md` | open | Whoever owns the hyaluronic-acid-vs-filler disposition (see item 3) |
| 2 | **Naming collision**: open PR #12 ("v3: conversational rewrite, human subject, 17-scene restructure") and `wip/hyaluronic-acid-video-rewrite-1ab509-2026-09-04`'s own report *also* call themselves "v3" of the same video (120.359s, 14 scenes, single narrator, retention-optimized) — two unrelated efforts independently claiming the same version label. | both | PR #12 title; `BEFORE-AFTER-REPORT.md` | open | Kim, before either merges — version numbering needs to be reconciled regardless of which content ships |
| 3 | **Three independent, uncoordinated rewrites of `hyaluronic-acid-vs-filler`** in flight at once (PR #12, the rewrite-1ab509 WIP, and the main tree's own WIP sitting on top of the already-merged v2) — all touch the same core files (VO tracks, beat sheet, build scripts, composition index). Not a git conflict to resolve mechanically; a product/creative decision on which version (or blend) ships. Full detail and file-overlap evidence in `docs/wo/BRANCH-REGISTER.md` Cluster 2. | `claude/hyaluronic-acid-video-rerender-8abe8c`, `wip/hyaluronic-acid-video-rewrite-1ab509-2026-09-04`, `wip/main-tree-2026-09-04` | T2 analysis | open | Kim |
| 4 | **Two independent, diverged, full rebuilds of `ectoin-survival-molecule`** — near-total file overlap, neither branch is an ancestor of the other, both forked from the same already-merged commit. Both are the two agents this session tracked as active and later confirmed finished. Commit messages suggest a possible intended sequence ("Step 1: build the mechanism" → "rebuild scene timing... for retention") but git history shows parallel independent work, not a continuation. Full detail in `docs/wo/BRANCH-REGISTER.md` Cluster 3. | `claude/ectoin-voice-timing-revision-825e2c`, `claude/voice-animation-sync-409a0a` | T2 analysis | open | Kim |
| 5 | `origin/rescue/34cf30b` was deleted from GitHub *during* this WO's Gate 0 read-only inventory pass — confirmed present at the start, gone by the end, via `git fetch --prune`. Not necessarily wrong (Kim may have done it deliberately), but unconfirmed, and it's exactly the kind of concurrent-mutation risk this repo's incident history warns about. The branch's content is superseded regardless (see `docs/wo/BRANCH-REGISTER.md` Cluster 1), so this doesn't block anything — it just needs a yes/no. | `rescue/34cf30b` | `docs/wo/GATE0-2026-09-04.md` §5 | open | Kim |
| 6 | Two worktrees live outside the `.claude/worktrees/` convention, directly under `~/Desktop`: `storyboard-6a` (branch already merged via PR #13, plus one still-untracked file now protected in `wip/storyboard-6a-2026-09-04`) and `storyboard-master` (a direct checkout of `master` itself). Neither is broken, but both are one accidental `rm -rf` or confused session away from being either lost or a source of "which checkout is this" confusion. | n/a (worktree layout, not branch content) | `docs/wo/GATE0-2026-09-04.md` §3 | open | Kim — decide whether to fold into the standard convention or leave as-is |
| 7 | Naming footgun: the worktree directory `ingredient-pages-master-f9c42b` is checked out on branch `claude/snail-mucin-bottle-animation-1d3308` (PR #14) — *not* a branch named after the directory. A same-named local branch `claude/ingredient-pages-master-f9c42b` does exist, but it's a different, already-redundant ref (verified ancestor of master). Same pattern with worktree `snail-mucin-video-feedback-d3484b` (checked out on `claude/ectoin-explainer-rebuild-ad081f`) vs. the look-alike orphaned branch `claude/snail-mucin-video-feedback-d3484b`. Risk: deleting "the branch that matches this worktree's name" by name alone deletes the wrong, unrelated ref. | n/a (naming, not content) | `docs/wo/GATE0-2026-09-04.md` §4 | open | Whoever runs T9 (cleanup) — read this before deleting anything by name |
| 8 | Expected (likely mechanical) merge conflict: `claude/kbeauty-ingredient-video-675976` and `claude/faceless-video-feedback-6c6c24` both modify the same two append-style channel logs, `videos/_channel/policy-change-proposals.md` and `videos/_channel/spend.jsonl`. Probably a concatenate-both resolution, not a real disagreement — flagged so T5's merge sequencing expects it rather than treating it as a surprise. | `claude/kbeauty-ingredient-video-675976`, `claude/faceless-video-feedback-6c6c24` | T2 analysis | open | Whoever runs T5 (integration) |
| 9 | 3 blobs >50MB in this repo's history (`videos/snail-mucin-medical-secret/renders/*.mp4`), predating the Git LFS migration — fossilized, not currently growing. A history rewrite would be needed to remove them. Explicitly out of scope for WO-SBR-001 (its own §5) — listed here only so it isn't rediscovered as a surprise later. | n/a | `docs/wo/GATE0-2026-09-04.md` §10 | open, out of scope | Separate WO if ever prioritized |

## Verification note (Rule 6)

Every item above was checked against the actual diff/content before being
listed as `open` — none are carried over from a branch's own self-report
without independent confirmation (e.g. item 1's disclosed limitation was
read directly from the source file, not summarized secondhand; items 3-4's
overlaps were confirmed via `diff --name-only` and `merge-base --is-ancestor`,
not inferred from branch names or commit messages alone).
