Subject card — the ingredient itself: name, INCI name, one line of function.

```jsx
<ShIngredient name="Niacinamide" inci="Niacinamide" function="Reduces sebum output and repairs the barrier." />
```

`accent` puts clay on the name; only use it when the scene spends its accent here rather than on a hook word.
