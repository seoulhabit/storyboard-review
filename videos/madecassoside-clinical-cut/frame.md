---
version: alpha
name: Madecassoside Clinical Cut — Frame
description: >
  Canonical SeoulHabit house tokens applied directly (same set as pdrn-cellular-science
  frame.md Round 4), with the dark stage-box dropped project-wide — every beat sits on the
  paper canvas, mirroring how PDRN itself treats four of its six beats. Portrait only (1080×1920).
unit: the frame — 1080×1920, 9:16 only
principle: one aqua accent per frame · coral is the one voltage moment · an UNSOURCED flag is
  never mistaken for a citation · no dark presenter stage anywhere in this cut · the presenter
  is one continuous object, drawn in ink, not staged in a dark panel
colors:
  ink: "#131516"
  paper: "#F7F5F0"
  card: "#FBF9F5"
  aqua: "#59B8AE"
  aqua-text: "#3A7871"       # aqua darkened for 3:1 large-text contrast on paper — required whenever
                             # aqua colors actual readable text, not just an underline/diagram line
  coral: "#C97A5C"
  coral-text: "#9F6149"
  ink-muted: "rgba(19,21,22,0.62)"
  line: "rgba(19,21,22,0.12)"
typography:
  headline-kinetic: { fontFamily: "Inter", weight: 800, upper: true }
  frame-line: { fontFamily: "EB Garamond", italic: true }
  fields-claims: { fontFamily: "Inter", weight: "500-700", note: "24px-equivalent floor, Round 4 scale" }
---

# Madecassoside Clinical Cut — Frame

- Safe areas: identical to flat-matrix — captions top band y=196; content band
  y ∈ [384,1440]; diagram field centered 640px wide (right ~15% rail clear);
  claim cards 822px at x=129.
- Diagram field: hairline-bordered (`1px solid {colors.line}`) directly on
  paper — not a filled dark panel. `[Authored, illustrative — not a claim]` tag
  bottom-left on every illustrative state, same convention as every sibling.
- Fiber field: same 14 quadratic strands from LCG seed 20260829, ported
  verbatim from flat-matrix — regenerated identically wherever it recurs, so
  it stays the same object. Rendered ink (`#131516`) at rest / disordered,
  aqua (`#59B8AE`) once realigned in beat 05 — never red; red belongs to
  flat-matrix's dark register, not this one.
- Motion: `power3.out` settles everywhere except beat 05's `back.out(1.4)`
  snap — the video's one licensed overshoot, unchanged from house precedent.
- Frame zero = the fully composed hook (also the visual landing pad for a
  replay, though beat 7 is a real CTA sentence, not an audio loop fragment).
- Known deviation from flat-matrix: no `#0D1112` stage-box, no damaged-red
  token — this cut has no dark register at all, per the brief's explicit
  "off-white backgrounds… strip away all chaotic sci-fi" direction.
