Mapped list — the workhorse; appears in four of the six templates. Left term in Archivo, brass hairline, right term in DejaVu Serif Bold.

```jsx
<ShRows rows={[{left:'Dullness',right:'Vitamin C'},{left:'Texture',right:'PHA'}]} active={0} />
```

Rows stagger in 0.8s apart and none leave; already-revealed rows sit at 60% opacity. `active` marks exactly one clay row. Three to five rows — past five, it is two scenes.
