A vs B — two columns split at the centre by a brass rule.

```jsx
<ShCompare a="Ascorbic acid" b="THD ascorbate" attributes={[{label:'Stability',aValue:'Low',bValue:'High',favours:'b'}]} />
```

Three attribute rows maximum. Clay marks the side that wins that attribute — never both, never a tick or a cross.
